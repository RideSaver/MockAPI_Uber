apiVersion: v1
kind: Service
metadata:
    name: uber-mock
    namespace: mock
    labels:
        type: api-mock
        mock: uber
spec:
    ports:
        - port: 80
          name: http
        - port: 443
          name: https
    selector:
        type: api-mock
        mock: uber
