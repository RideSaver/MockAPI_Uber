apiVersion: v1
kind: Service
metadata:
  name: {{ include "RideSaver_Mock_Uber.fullname" . }}
  labels:
    {{- include "RideSaver_Mock_Uber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "RideSaver_Mock_Uber.selectorLabels" . | nindent 4 }}
